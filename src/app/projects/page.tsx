import { Composition } from "@/components/section";

const Projects: React.FC = () => {
  return (
    <Composition.section>
      <Composition.title title="/Meus projetos"/>
    </Composition.section>
  );
};

export default Projects;
