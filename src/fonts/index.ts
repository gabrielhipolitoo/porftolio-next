import { Work_Sans } from "next/font/google";

export const work_sans_bold = Work_Sans({ weight: "700", style: ["normal"] });
export const work_sans_normal = Work_Sans({ weight: "400", style: ["normal"] });
export const work_sans_light = Work_Sans({ weight: "300", style: ["normal"] });
